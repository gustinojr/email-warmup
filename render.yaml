services:
  - type: web
    name: email-warmup
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python warmup_sender.py"
    envVars:
      - key: RESEND_API_KEY
        sync: false
